# Minime cores — pre-built aarch64 libretro cores.
# Initially copied from the rgb30 (RK3566) build.
# CI populates this directory before the zip is assembled.

all:
	@echo "cores: nothing to build (pre-built, populated by CI)"

clean:
	rm -rf output
