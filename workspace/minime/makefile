# Minime platform makefile.
# Minimal: no third-party deps to build (Minime provides SDL2 and tools).
# Cores are pre-built aarch64 binaries copied from the rgb30/rk3566 build.

all:
	cd show && make

early:

clean:
	cd show && make clean
